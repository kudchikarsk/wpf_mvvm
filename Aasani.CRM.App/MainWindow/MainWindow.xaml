<Window x:Class="Aasani.CRM.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:Behaviors="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:local="clr-namespace:Aasani.CRM.App" 
        mc:Ignorable="d"
        Title="Aasani CRM" Height="450" Width="800">

    <Window.DataContext>
        <local:MainWindowViewModel/>
    </Window.DataContext>
    <Behaviors:Interaction.Triggers>
        <Behaviors:EventTrigger EventName="Loaded">
            <Behaviors:CallMethodAction TargetObject="{Binding}" MethodName="OnLoad"/>
        </Behaviors:EventTrigger>
    </Behaviors:Interaction.Triggers>
    
    <StackPanel Orientation="Horizontal">
        <StackPanel>
            <ListView Margin="10" ItemsSource="{Binding Contacts}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <WrapPanel>
                            <TextBlock Text="{Binding FirstName}" FontWeight="Bold" />
                            <TextBlock Text="{Binding LastName}" FontWeight="Bold" />
                            <TextBlock Text="{Binding Mobile}" />
                        </WrapPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
        <StackPanel>
            <TextBox Text="{Binding NewContact.FirstName}" Margin="0,5,0,0" />
            <TextBox Text="{Binding NewContact.LastName}" Margin="0,5,0,0" />
            <TextBox Text="{Binding NewContact.Mobile}" Margin="0,5,0,0" />
            <Button Margin="10,10,0,0" Command="{Binding AddContact}">
                <StackPanel Orientation="Horizontal">
                    <TextBlock FontFamily="Segoe MDL2 Assets" >&#xE710;</TextBlock>
                    <TextBlock><Run Text="Add Customer"/></TextBlock>
                </StackPanel>
            </Button>

        </StackPanel>
    </StackPanel>
</Window>
