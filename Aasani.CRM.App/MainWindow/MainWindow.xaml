<Window x:Class="Aasani.CRM.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:Behaviors="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:local="clr-namespace:Aasani.CRM.App" 
        mc:Ignorable="d"
        Title="Aasani CRM" Height="450" Width="800"
        local:ViewModelLocator.AutoWireViewModel="True">

    <!--<Window.DataContext>
        <local:MainWindowViewModel/>
    </Window.DataContext>
    <Behaviors:Interaction.Triggers>
        <Behaviors:EventTrigger EventName="Loaded">
            <Behaviors:CallMethodAction TargetObject="{Binding}" MethodName="OnLoad"/>
        </Behaviors:EventTrigger>
    </Behaviors:Interaction.Triggers>-->
    
    <StackPanel Orientation="Horizontal">
        <StackPanel>
            <ListView Margin="10" ItemsSource="{Binding Contacts}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <WrapPanel Margin="10">
                            <TextBlock Text="{Binding FirstName}" FontWeight="Bold" />
                            <TextBlock Text="{Binding LastName}" FontWeight="Bold" Margin="5,0,0,0" />
                            <TextBlock Text="{Binding Mobile}" Margin="5,0,0,0" />
                        </WrapPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
        <StackPanel>
            <TextBlock Text="FirstName" FontWeight="Bold"  Margin="0,10,0,0"/>
            <TextBox Text="{Binding NewContact.FirstName}" Padding="5" Margin="2"/>
            <TextBlock Text="LastName" FontWeight="Bold" Margin="0,5,0,0" />
            <TextBox Text="{Binding NewContact.LastName}" Padding="5" Margin="2"/>
            <TextBlock Text="Mobile" FontWeight="Bold" Margin="0,5,0,0"/>
            <TextBox Text="{Binding NewContact.Mobile}" Padding="5" Margin="2"/>
            <Button Margin="10,10,0,0" Command="{Binding AddContact}">
                <StackPanel Orientation="Horizontal" Margin="5">
                    <TextBlock FontFamily="Segoe MDL2 Assets" Margin="0,2,5,0" >&#xE710;</TextBlock>
                    <TextBlock><Run Text="Add Customer"/></TextBlock>
                </StackPanel>
            </Button>

        </StackPanel>
    </StackPanel>
</Window>
